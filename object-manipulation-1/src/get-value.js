/* exported getValue */
/*
  - define a function named getValue with two parameters "object" and "key"
  - get the value from the property provided as key using object at key
  - return output
*/

function getValue(object, key) {
  return object[key];
}
