/* exported take */
/*
  - create a function named take with two parameters "array" and "count"
  - using the slice() method starting from the index of 0 to the count value
  - return new array
*/

function take(array, count) {
  return array.slice(0, count);
}
