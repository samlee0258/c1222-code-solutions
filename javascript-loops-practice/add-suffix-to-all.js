/* exported addSuffixToAll */
/*
  - make a variable that holds two parameters within its parenthesis the words and suffix
  - assign an empty array to a new variable
  - make a for loop to loop through an array filled with strings
  - have the for loop check each index in the array
  - at each index of the array write a code inside the for loop code block to
  spit out each individual string at the index number and combine it with the suffix
  - push the strings with their new suffix into the newly assigned variable with the empty array
  - return the new array with the new strings
*/
