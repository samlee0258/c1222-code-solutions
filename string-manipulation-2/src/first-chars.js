/* exported firstChars */
/*
  - create a function named firstChars with two parameters "length" and "string"
  - using the slice() method cut the string from 0 to the "length" value
  - return shortened string
*/

function firstChars(length, string) {
  return string.slice(0, length);
}
