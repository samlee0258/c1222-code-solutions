/* exported lastChars */
/*
  - create a function named lastChars with two parameters "length" and "string"
  - create a variable and assign it to an empty array
  - create a for loop to loop through the string
    - loop the characters from the string backwards
    - start from the string.length -1
      - if the string.length -1 === the length of the parameter
    - push the characters into output array
  - return output array
*/
